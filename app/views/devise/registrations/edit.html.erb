<h2>Редагування облікового запису: <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>

  <div class="col-md-4 col-md-offset-4">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, :class => 'form-control' %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="col-md-4 col-md-offset-4">
    <%= f.label 'Новий пароль:' %> <i>(залиште пустим, якщо не хочете змінювати)</i><br />
    <%= f.password_field :password, autocomplete: "off", :class => 'form-control' %>
  </div>

  <div class="col-md-4 col-md-offset-4">
    <%= f.label "Підтвердження паролю:" %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off", :class => 'form-control' %>
  </div>

  <div class="col-md-4 col-md-offset-4">
    <%= f.label "Поточний пароль:" %> <i>(він нам потрібен, щоб впевнитися, що це саме ви)</i><br />
    <%= f.password_field :current_password, autocomplete: "off", :class => 'form-control' %>
  </div>
 <div class="col-md-4 col-md-offset-4">
  <div class="actions">
    <%= f.submit  "Update", :class => 'btn btn-default' %>
  </div>
    <%= link_to "Back", :back %>
  </div>
<% end %>
<div class='row'>

</row>
<div class='row'>
<h3>Видалити мій обліковий запис</h3>

<p>Незадоволені порталом? <%= button_to "Видалити обліковий запис", registration_path(resource_name), data: { confirm: "Ви впевненні?" }, method: :delete,  :class => 'btn btn-danger'  %></p>

